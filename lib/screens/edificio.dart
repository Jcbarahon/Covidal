import 'package:flutter/material.dart';

class EdificioPage extends StatelessWidget {
  const EdificioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Edificio')));
  }
}
